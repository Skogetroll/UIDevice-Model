# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'UIDevice-Model' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for UIDevice-Model

  target 'UIDevice-ModelTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'UIDevice-ModelPackageDescription' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for UIDevice-ModelPackageDescription

end
